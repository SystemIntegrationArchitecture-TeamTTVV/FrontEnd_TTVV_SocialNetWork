/** Story image/video — align with backend `StoriesService` max. */
export const STORY_MEDIA_MAX_BYTES = 10 * 1024 * 1024; // 10 MB
export const STORY_MEDIA_MAX_MB = 10;

/** Video post — align with backend `VideoController` max. */
export const VIDEO_MAX_BYTES = 5 * 1024 * 1024; // 5 MB
export const VIDEO_MAX_MB = 5;
